# Dependencies for "Why the Bayesian Bootstrap?"
#
# Install into the post's own virtualenv, never the system Python:
#   python3 -m venv ../../.venv-bayesian-bootstrap
#   ../../.venv-bayesian-bootstrap/bin/python -m pip install -r requirements.txt
# or just `make venv`, which also registers the Jupyter kernel.

# analysis
numpy==2.5.1
scipy==1.18.0
pandas==3.0.5
matplotlib==3.11.1
pyarrow==25.0.0
requests==2.34.2
pillow==12.3.0

# MNIST. torch is the only supported backend for the weighted likelihood
# bootstrap; there is deliberately no silent fallback to a different model.
torch==2.13.0
torchvision==0.28.0

# Quarto's execution stack. ipykernel alone is not enough — without nbclient,
# nbformat and pyyaml a render dies with ModuleNotFoundError: No module named
# 'yaml' from Quarto's jupyter.py shim.
jupyter==1.1.1
ipykernel==7.3.0
nbclient==0.11.0
nbformat==5.10.4
PyYAML==6.0.3

# pandas.DataFrame.to_markdown, used for every table in the post
tabulate==0.10.0
