# Dependencies for "Explainability Is a Localization Problem"
#
# Install into the post's own virtualenv, never the system Python:
#   uv venv .venv-explainability
#   uv pip install --python .venv-explainability/bin/python \
#       -r posts/explainability-localization/requirements.txt
#   .venv-explainability/bin/python -m ipykernel install \
#       --user --name explainability-blog
#
# Run from the repo root. This is a curated list of direct dependencies, not a
# generated lock — keep it that way.

# analysis
numpy==2.5.1
scipy==1.18.0
pandas==3.0.5
matplotlib==3.11.1
pillow==12.3.0

# Part II. scikit-learn supplies Iris, PCA, logistic regression and permutation
# importance; statsmodels is the cross-check the post asserts its hand-derived
# standard errors against.
scikit-learn==1.9.0
statsmodels==0.14.6

# Part I. torch is the only supported backend for the MNIST classifier and every
# attribution method in the post; there is deliberately no fallback.
torch==2.13.0
torchvision==0.28.0

# Quarto's execution stack. ipykernel alone is not enough — without nbclient,
# nbformat and pyyaml a render dies with ModuleNotFoundError: No module named
# 'yaml' from Quarto's jupyter.py shim.
jupyter==1.1.1
ipykernel==7.3.0
nbclient==0.11.0
nbformat==5.10.4
PyYAML==6.0.3

# pandas.DataFrame.to_markdown, used for every table in the post
tabulate==0.10.0
