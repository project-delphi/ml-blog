# The NIST StRD file is committed exactly as published, CRLF line endings and
# all, so the cached copy stays byte-identical to the certified original.
# `src/data.py` parses it with splitlines(), which handles either ending.
assets/Filip.dat -text
